{
  "name": "l2-billing-balance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ l2-billing-balance",
    "create-local": "graph create --node http://localhost:8020/ l2-billing-balance",
    "remove-local": "graph remove --node http://localhost:8020/ l2-billing-balance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 l2-billing-balance",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
